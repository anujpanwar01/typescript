/**
 * Tuples all about the index of element type
 **/

const rgb: [number, number, number] = [255, 255, 255];
// rgb = ["123", 1232, 21312]; // giving me error because only the number allowed

let myTuples: [boolean, number, string] = [true, 2, "anuj"];
// myTuples = ["anuj", 2, true]; // i can't reverse the order of myTuples

myTuples.push(true); // value should'nt push into myTuples here is catch in tuples
